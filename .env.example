# GoHighLevel inbound webhook URL the /api/lead function forwards leads to.
# Create it in GHL (Automation → Workflows → Webhook trigger) and paste here.
# If unset, /api/lead just logs the lead and returns success.
GHL_WEBHOOK_URL=
